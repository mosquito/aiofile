[project]
name = "aiofile-benchmark"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiofile",
    "aiofiles",
    "aiomisc",
]

[tool.uv.sources]
aiofile = { path = "..", editable = true }
