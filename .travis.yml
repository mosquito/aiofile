language: python
cache: pip
python:
  - '3.4'
  - '3.5'
  - '3.6-dev'
  - 'nightly'
install:
  - pip install -U cython pytest pytest-cov pytest-asyncio
  - pip install -Ue .
script:
  - python -c "import sys; print(sys.platform)"
  - py.test --cov=aiofile tests
  - pip uninstall -y .
